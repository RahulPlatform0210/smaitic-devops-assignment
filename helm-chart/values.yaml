replicaCount: 2

image:
  repository: smaitic-api
  tag: v4
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: smaitic.local

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"

  requests:
    cpu: "250m"
    memory: "256Mi"